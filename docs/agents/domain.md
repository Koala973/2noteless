# Domain Docs

This repo is single-context.

- Domain language lives in root `CONTEXT.md` when present.
- Architecture decisions live in `docs/adr/`.
- If those files do not exist, inspect code and current plans first; do not invent domain terms.
